// Local stub of DOMPurify 2.4.0: minimal sanitize that returns input as-is.
// This avoids CDN fetch failures and syntax errors while keeping API surface.
// If you need full sanitization, replace this file with the official build.
(function(global){
	var DOMPurify = {
		sanitize: function(input){ return input; }
	};
	if (typeof module !== 'undefined' && module.exports) {
		module.exports = DOMPurify;
	}
	if (typeof define === 'function' && define.amd) {
		define(function(){ return DOMPurify; });
	}
	global.DOMPurify = DOMPurify;
})(typeof globalThis !== 'undefined' ? globalThis : (typeof window !== 'undefined' ? window : this));
